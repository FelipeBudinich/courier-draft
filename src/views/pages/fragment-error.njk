<section class="panel">
  <h2 class="text-xl text-ink">{{ title }}</h2>
  <p class="mt-2 text-sm leading-6 text-ink/65">{{ message }}</p>
  {% if requestId %}
    <p class="mt-3 text-xs text-ink/45">{{ t('shared.labels.requestId') }}: {{ requestId }}</p>
  {% endif %}
</section>
