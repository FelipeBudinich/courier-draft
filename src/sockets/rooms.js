export const roomHelpers = {
  user(userId) {
    return `user:${userId}`;
  },
  project(projectId) {
    return `project:${projectId}`;
  },
  script(scriptId) {
    return `script:${scriptId}`;
  },
  scene(sceneId) {
    return `scene:${sceneId}`;
  },
  note(noteId) {
    return `note:${noteId}`;
  }
};

